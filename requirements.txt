#
# Pinned lock file, generated by pip-compile from requirements.in - direct dependencies live in
# pyproject.toml's [project.dependencies], unpinned; this file pins the full closure (direct +
# transitive) to the exact versions this codebase's test suite (845 tests) is known to pass
# against, so a fresh checkout reproduces the same environment rather than whatever's newest on
# PyPI that day. Regenerate deliberately (`pip-compile requirements.in`), re-run the full test
# suite, and re-pin to what's actually validated - don't just take pip-compile's freshest
# resolution on faith.
#
-e .
attrs==26.1.0
contourpy==1.2.1
cycler==0.12.1
exceptiongroup==1.3.0
fonttools==4.51.0
iniconfig==2.0.0
jsonschema==4.26.0
jsonschema-specifications==2025.9.1
kiwisolver==1.4.5
matplotlib==3.8.4
numpy==2.2.6
packaging==24.0
pillow==10.3.0
pluggy==1.4.0
pyarrow==25.0.1
pyparsing==3.1.2
pytest==8.1.1
python-dateutil==2.9.0.post0
referencing==0.37.0
rpds-py==0.30.0
six==1.16.0
tomli==2.2.1
typing-extensions==4.13.2
